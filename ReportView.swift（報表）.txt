import SwiftUI

struct ReportView: View {
    @ObservedObject var vm: StoreViewModel
    
    var body: some View {
        VStack(spacing: 20) {
            Text("📊 銷售報表").font(.largeTitle)
            Text("總銷售額：$\(vm.totalSales)")
            Text("總利潤：$\(vm.totalProfit)")
        }
        .padding()
    }
}
