workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 60
    environment:
      vars:
        FLAVOR: "prod"
    scripts:
      - name: Install dependencies
        script: |
          echo "No dependencies to install"
      - name: Build app
        script: |
          xcodebuild -workspace WelfareStoreApp -scheme WelfareStoreApp -sdk iphoneos -configuration Release clean build
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          -a098155513269@gmail.com
